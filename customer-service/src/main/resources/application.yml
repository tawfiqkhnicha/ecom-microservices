server:
  port: 8080
spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:h2:mem:customer-db
  h2:
    console:
      enabled: true
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false